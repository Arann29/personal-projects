services:
  db:
    image: postgres:16
    container_name: sentiment_nlp_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sentimentnlp
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/db/init.sql:/docker-entrypoint-initdb.d/init.sql

  api:
    build: .
    container_name: sentiment_nlp_api
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/sentimentnlp
    ports:
      - "8003:8000"
    depends_on:
      - db
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

  dashboard:
    build: .
    container_name: sentiment_nlp_dashboard
    env_file:
      - .env
    ports:
      - "8504:8501"
    depends_on:
      - api
    command: streamlit run dashboard/app.py --server.port=8501 --server.address=0.0.0.0

volumes:
  postgres_data:
